apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: multi-main
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/CharanReddy129/multi-env-node.git'
    targetRevision: main
    path: ./multi-env
    helm:
      valueFiles:
        - values-prod.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    ignoreDifferences:
    - group: storage.k8s.io
      kind: StorageClass
      name: multi 